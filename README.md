# Библиотечный ИИ-Поиск (RAG + SQL Search)

Система для интеллектуального поиска по библиотечным фондам, объединяющая классический точный поиск по базе данных (SQL) и современный семантический поиск (RAG) с анализом PDF-книг.

## 🚀 Возможности
- **Smart Поиск (RAG)**: Поиск по смыслу во всей библиотеке с ответами от нейросети.
- **Точный Поиск (SQL)**: Поиск по автору, названию, ББК, ГРНТИ и кодам.
- **Анализ PDF**: Автоматическая выгрузка текста из PDF-файлов (первых 40 страниц) и создание краткого содержания (Summary) с помощью ИИ.
- **Два интерфейса**: Telegram-бот и Web-интерфейс.

## 📂 Структура проекта
```
backend/
├── app/
│   ├── main.py             # Точка входа FastAPI, API эндпоинты
│   ├── bot/                # Telegram бот (логика и обработчики)
│   ├── services/           # Бизнес-логика (SQLService, RAGSystem)
│   ├── core/               # Ядро (LLM-клиент, конфиги)
├── scripts/                # Скрипты инициализации и импорта данных
├── static/                 # Фронтенд (HTML, JS)
├── chromadb_store/         # Хранилище векторной базы данных
├── .env                    # Настройки API, БД и пути к моделям
└── requirements.txt        # Зависимости Python
```

## 🛠 Установка и запуск

### 1. Подготовка окружения
- Установите Python 3.10+
- Установите зависимости:
  ```bash
  pip install -r requirements.txt
  ```
- Настройте файл `.env`. Убедитесь, что указаны:
  - `TELEGRAM_BOT_TOKEN`
  - `DB_USER`, `DB_PASS`, `DB_NAME` (PostgreSQL)
  - `LLM_API_BASE` (путь к локальному LM Studio или OpenAI API)

### 2. Импорт данных
Перед запуском нужно наполнить базы данных информацией из ваших каталогов (excel/json).

1. **Импорт в SQL** (для точного поиска):
   ```bash
   python scripts/2_import_sql.py
   ```
2. **Индексация в RAG** (для умного поиска):
   ```bash
   python scripts/3_ingest_fulltext.py
   ```

### 3. Запуск приложения
Приложение запускает основной веб-сервер и Telegram-бота одновременно.
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000
```
- **Web UI**: доступен по адресу `http://localhost:8000`
- **Telegram Bot**: начнет принимать сообщения автоматически после пуска.

## 🧪 Используемые технологии
- **Backend**: FastAPI, Python-Telegram-Bot (telebot).
- **Базы данных**: PostgreSQL (SQL), ChromaDB (Векторная).
- **AI/ML**: Sentence-Transformers, OpenAI-Compatible API (Qwen/ChatGPT).
- **PDF**: PyMuPDF (fitz), PyPDF (fallback).

---
*Разработано в рамках проекта по автоматизации университетской библиотеки.*
